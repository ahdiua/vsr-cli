# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Runtime / config (machine-specific)
# setup.sh installs the venv, plugins, models and downloads here by default
# (in-repo so it can be captured in an image snapshot); never commit it.
vsr-runtime/
config.toml
*.engine
*.engine.cache
*.onnx

# OS
.DS_Store
Thumbs.db
